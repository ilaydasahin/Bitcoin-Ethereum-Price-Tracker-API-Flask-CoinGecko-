# Development & analysis dependencies (API runtime deps are in requirements.txt)
-r requirements.txt

# Testing
pytest>=8.0.0

# Jupyter notebook analysis environment
jupyter>=1.0.0
plotly>=5.18.0
pycoingecko>=3.1.0
mplfinance>=0.12.10b0
numpy>=1.26.0
